<ol class="dd-list">
  <% @members.zip(@positions, 0..(@members.count)).each do |member, collection_position, member_counter| %>
    <%= render partial: 'member', locals: {member: member, collection_position: collection_position, member_counter: member_counter, form: form} %>
  <% end %>
</ol>

