<dl class="document-metadata dl-horizontal dl-invert">
  <% if document.description.present? %>
    <dd class="blacklight-description_tesim"><%= truncate(document.description.join(', '), length: 85, separator: ' ') %></dd>
  <% end %>
</dl>
