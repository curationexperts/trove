<dl class="document-metadata dl-horizontal dl-invert">
  <% if document.description.present? %>
    <dd class="blacklight-description_tesim"><%= document.description.join(', ').truncate(90, separator: ' ') %></dd>
  <% end %>
</dl>
